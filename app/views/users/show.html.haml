%h1= @user.login

.q 
  Die von Dir hier angegebenen Daten werden von uns streng vertraulich 
  behandelt und weder auf dieser Website ver&ouml;ffentlicht noch an 
  Dritte weitergegeben.

%h2 Deine Daten

.q
  %b Name:
  = @user.login 

.q
  %b Email:
  = @user.email 

.q
  %b PLZ:
  = @user.zip 

- unless @organisations.empty?
  %h2 Deine Initiatoren
  .q= @organisations.map{|organisation| link_to organisation.title, organisation}.join(', ')

- unless @events.empty?
  %h2 Deine Demonstrationen
  .q= @events.map{|event| link_to event.title, event}.join(', ')

%h2 Deine Abonnements

%h3 Themen
.q= @user.tags.map{|tag| link_to tag.name, tag}.join(', ')

%h3 Demos & Inititativen
.q
  %ul
    - @user.bookmarks.each do |bm|
      %li= link_to bm.title, bm.bookmarkable

.contextmenu
  =link_to 'Bearbeiten', edit_user_path(@user)
