%h1 Initiatoren

%table{:border=>0, :cellspacing=>0, :cellpadding=>0}
  %tr
    %th Initiator
    %th Letzte Demo
    %th N&auml;chste Demo
  - @organisations.sort_by(&:title).each_with_index do |organisation, index|
    %tr{:class => "#{'even' if index.even?}"}
      %td= link_to organisation.title, organisation 
      %td 
      %td 

%br

.contextmenu
  = link_to 'Neuer Initiator', new_organisation_path
